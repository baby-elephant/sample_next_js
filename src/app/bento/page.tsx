export default function BentoPage() {
  return (
    <div>
      <h1>Parallel Routes: Bento Demo</h1>
      <p>
        This header comes from the default slot (page.tsx). The two panels
        below are independent named slots.
      </p>
    </div>
  );
}
